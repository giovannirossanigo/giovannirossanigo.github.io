name: Build TikZ diagrams

on:
  push:
    branches:
      - master
    paths:
      - 'diagrams/*.tex'
      - '.github/workflows/build-diagrams.yml'

jobs:
  build-diagrams:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.PAT_TOKEN }}

      - name: Install TeXLive + Inkscape
        run: |
          sudo apt-get update
          sudo apt-get install -y texlive-full inkscape

      - name: Compile TikZ diagrams
        run: |
          mkdir -p assets/img/diagrams
          for f in diagrams/*.tex; do
            pdflatex -interaction=nonstopmode -halt-on-error -output-directory=diagrams "$f"
            inkscape diagrams/$(basename "$f" .tex).pdf --export-plain-svg=assets/img/diagrams/$(basename "$f" .tex).svg
          done

      - name: Commit and push changes
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add assets/img/diagrams/*.svg
          git commit -m "Update TikZ diagrams [skip ci]" || echo "No changes to commit"
          git push origin master
